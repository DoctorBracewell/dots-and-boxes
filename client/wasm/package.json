{
	"type": "module",
	"scripts": {
		"build": "wasm-pack build --target web --color always",
		"postbuild": "node update_package_json.mjs"
	},
	"devDependencies": {
		"prettier": "^2.8.3",
		"prettier-plugin-tailwindcss": "^0.2.2"
	}
}
